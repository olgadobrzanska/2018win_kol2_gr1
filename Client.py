class Client:
	def __init__(self, name, username):
        self.name = name
        self.username = username
        self.money = 0


    def cash(self,value)
    	if self.money = 0
    		print("Don't have money")

   	def add_client(self, client)
    	self.client = self.append(client)

    
